akka {
  loglevel = "INFO"
  actor {
    provider = "akka.remote.RemoteActorRefProvider"
  }
  remote {
    enabled-transports = ["akka.remote.netty.tcp"]
    netty.tcp {
      hostname = "150.145.111.216"   //insert IP of the Pc in which is executed
      port = 5150
    }
    log-sent-messages = on
    log-received-messages = on
  }
}